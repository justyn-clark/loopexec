small_version: "1.0.0"
owner: "human"
intent: "Prepare loopexec as a public bounded-loop execution CLI companion to SMALL Protocol."
scope:
  include:
    - "loopexec CLI contract"
    - "public release metadata"
    - "repository documentation"
  exclude:
    - "internal worker prototypes"
    - "runtime logs"
    - "local machine state"
success_criteria:
  - "Repository has a clean public README and license."
  - "Only public release files are tracked."
  - "Tests and secret scan pass before publication."
