# Spindle
Spindle is a CLI that drives work based on SMALL , state a loop runner that enforces boundaries a substrate executor that makes environments reproducible an integration surface that other tools can hook into (emit --json)
